<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow plain HTTP to local network addresses (192.168.x.x, 10.x.x.x, etc.) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
